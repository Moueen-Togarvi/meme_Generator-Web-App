## Django
# SECURITY: set a strong random value in production (never commit real secrets)
DJANGO_SECRET_KEY=change-me

# true/false/1/0
DJANGO_DEBUG=false

# Comma-separated
DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost

# Optional (comma-separated). Example:
# DJANGO_CSRF_TRUSTED_ORIGINS=https://your-app.vercel.app,https://your-domain.com
DJANGO_CSRF_TRUSTED_ORIGINS=

## Database
# For local dev you can omit DATABASE_URL and sqlite will be used.
# Example Postgres:
# DATABASE_URL=postgres://USER:PASSWORD@HOST:5432/DBNAME
DATABASE_URL=

# true/false. Enable if your DB requires SSL (typical managed Postgres).
DATABASE_SSL_REQUIRE=false

